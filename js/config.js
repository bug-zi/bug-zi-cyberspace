// ============================================
// 配置中心：全局常量定义
// ============================================

export const CONFIG = {
  // 玩家配置
  PLAYER: {
    HEIGHT: 1.6,          // 玩家眼睛高度（米）
    RADIUS: 0.3,         // 玩家碰撞半径（米）
    MOVE_SPEED: 0.225,      // 基础移动速度（原0.15，再次提升1.5倍）
    SPRINT_MULTIPLIER: 2.0, // 冲刺速度倍数
    JUMP_FORCE: 0.15,     // 跳跃力度
    GRAVITY: -0.008,       // 重力加速度
  },

  // 房间配置
  ROOM: {
    WIDTH: 30,              // 房间宽度（米）
    DEPTH: 30,              // 房间深度（米）
    HEIGHT: 10,              // 房间高度（米）（原8米，增加2米）
    WALL_DEPTH: 0.01,       // 墙壁厚度（米）
  },

  // 门洞配置
  DOOR: {
    WIDTH: 4,              // 门洞宽度（米）
    HEIGHT: 6,             // 门洞高度（米）
  },

  // 碰撞检测配置
  COLLISION: {
    OFFSET: 0.4,           // 碰撞偏移量（米）
    PLAYER_SIZE: {           // 玩家尺寸（用于边界框）
      x: 0.6,              // 宽度的一半
      y: 0.8,              // 高度的一半
      z: 0.3               // 深度的一半
    }
  }
};

console.log('✅ 配置文件已加载');
